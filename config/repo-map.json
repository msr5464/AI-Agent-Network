{
  "_comment": "Per-repo metadata, keyed by GITHUB_REPO_AUTOMATION. Only `framework` is read (shared/frameworks/detect.py), as a fallback when the repo's build files do not settle it.",
  "Jarvis": {
    "framework": "TestNG + Playwright"
  },
  "Playwright-Automation-Framework": {
    "framework": "TestNG + Playwright"
  },
  "Selenium-Automation-Framework": {
    "framework": "TestNG + Selenium"
  }
}
