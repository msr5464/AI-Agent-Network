# Core Dependencies
python-dotenv>=1.0.0

# Database
pymysql>=1.1.0

# LLM and AI libraries
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
PyGithub>=2.1.1
GitPython>=3.1.40

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# HTTP requests
requests>=2.31.0
# Imported directly by the healing and triaging agents (disable_warnings), not
# just pulled in behind requests — a direct import belongs in the direct deps.
urllib3>=2.0.0

# Reads config/locator.yaml (the healing agent and locator_heal) and
# config/prompts.yaml.
PyYAML>=6.0

# Browser automation for the locator healing engine. locator_heal/ and the
# healing agent's 01_locate.py drive it directly. Needs `playwright install
# chromium` after pip, which pip cannot do for you.
playwright>=1.40.0

# Core Python libraries
pydantic>=2.0.0

# Optional - for web UI
streamlit>=1.28.0

# Audit dashboard + qa_agents_server HTTP+SSE wrapper
flask>=3.0.0
flask-cors>=4.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Browser automation for locator discovery
selenium>=4.15.0
