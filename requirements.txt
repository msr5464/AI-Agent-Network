# Core Dependencies
python-dotenv>=1.0.0

# Database (test-triaging-agent)
pymysql>=1.1.0

# HTML parsing (BeautifulSoup with the lxml parser)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# HTTP requests
requests>=2.31.0
# Imported directly by the triaging agent (disable_warnings), not just pulled in
# behind requests — a direct import belongs in the direct deps.
urllib3>=2.0.0

# Reads config/locator.yaml (the healing agent and locator-eval).
PyYAML>=6.0

# Browser automation for the locator healing engine. locator-eval/ and the
# healing agent's 01_locate.py drive it directly. Needs `playwright install
# chromium` after pip, which pip cannot do for you.
playwright>=1.40.0

# Audit dashboard + qa_agents_server HTTP+SSE wrapper
flask>=3.0.0
flask-cors>=4.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
